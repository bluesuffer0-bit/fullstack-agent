<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      No INTERNET permission, on purpose. The app is a four-button remote:
      everything it starts is a localhost server inside Termux, and the
      "open the face" button fires a VIEW intent that the browser handles
      with its own permissions. Asking for network it never uses is how an
      app earns a permission question it cannot answer.
    -->

    <!--
      Package visibility (Android 11+). Starting Termux's service and even
      asking whether Termux is installed is denied silently unless the
      package is named here. The browser intent is declared too, so the
      "open the face" button can find a browser when none is the default.
    -->
    <queries>
        <package android:name="com.termux" />
        <package android:name="com.termux.api" />
        <package android:name="com.termux.widget" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FullstackAgent">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:label="@string/settings_title"
            android:exported="false"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>
