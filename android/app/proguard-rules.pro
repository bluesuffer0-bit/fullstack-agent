# Keep the default file. The app ships with no reflection and no JNI, so
# there is nothing here that shrinking would need to be told about; this
# file exists so the release build type has a target to point at.
